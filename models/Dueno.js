class Dueno extends Usuario {
    constructor(id, name) {
       this.id = id;
       this.name = name;
       this.status = true;
       this.mascotas = []; 
    }

    cambiarName() {}

    cambiarStatus() {}

    registarMascota() {}

    cambiarStatusMascota() {}
}